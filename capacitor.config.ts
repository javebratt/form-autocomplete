import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'form-autocomplete',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
